import { CSSClasses } from "../../style/style-types";

const wordStyle: CSSClasses = {
  backBtn: {
    marginTop: ".5rem",
  },
  wordContainer: {
    marginTop: "2rem",
    maxWidth: "800px",
  },
};

export default wordStyle;
